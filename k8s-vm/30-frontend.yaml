# Frontend running inside a VMware VM Operator VirtualMachine (Ubuntu cloud image).
# cloud-init installs Node.js 20 + nginx, clones this repo, builds the SPA with
# `npm run build`, deploys the static assets to nginx, and installs the project's
# nginx.conf (which reverse-proxies /api/ to the `backend` host -- resolved by
# the bind9 DNS server in 05-dns.yaml).
#
# `frontend-lb` is a LoadBalancer VirtualMachineService that puts a public VIP
# in front of this VM on ports 80 (HTTP) and 22 (SSH). It's the only externally
# reachable endpoint of the application.


apiVersion: vmoperator.vmware.com/v1alpha5
kind: VirtualMachine
metadata:
    name: frontend
    labels:
        app: frontend
spec:
    # Adjust these to a class, image, and StorageClass that exist in your
    # Supervisor namespace -- see the README.
    className: best-effort-small
    # Friendly image name: noble-server-cloudimg-amd64
    imageName: ubuntu
    storageClass: vsan-default-storage-policy
    powerState: PoweredOn
    network:
        hostName: frontend
    bootstrap:
        cloudInit:
            rawCloudConfig:
                name: frontend-bootstrap
                key: user-data
