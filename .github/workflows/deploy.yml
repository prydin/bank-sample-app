name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: kubectl apply
        run: vcf context use vcfa:bank-vm-h93q9:default-project; kubectl apply -f k8s-vm/
